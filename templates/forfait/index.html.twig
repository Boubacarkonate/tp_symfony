{% extends 'base.html.twig' %}

{% block title %}Forfait index{% endblock %}

{% block body %}
    <h1>Forfait index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Prix</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for forfait in forfaits %}
            <tr>
                <td>{{ forfait.id }}</td>
                <td>{{ forfait.nom }}</td>
                <td>{{ forfait.duree }} mois</td>
                <td>{{ forfait.prix }} €</td>
                <td>
                    <a href="{{ path('app_forfait_show', {'id': forfait.id}) }}">show</a>
                    <a href="{{ path('app_forfait_edit', {'id': forfait.id}) }}">edit</a>
                    <a href="{{ path('app_cart_add', {'id': forfait.id}) }}">
                      <a href="{{ path('app_cart_add', {'id': forfait.id}) }}">
                    <button type="button" class="btn btn-primary">Ajouter au panier</button>
                    </a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_forfait_new') }}">Create new</a>
{% endblock %}
