{% extends 'base.html.twig' %}

{% block title %}Forfait{% endblock %}

{% block body %}
    <h1>Forfait</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ forfait.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ forfait.nom }}</td>
            </tr>
            <tr>
                <th>durée</th>
                <td>{{ forfait.duree }} mois</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ forfait.prix }} €</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_forfait_index') }}">back to list</a>

    <a href="{{ path('app_forfait_edit', {'id': forfait.id}) }}">edit</a>

    {{ include('forfait/_delete_form.html.twig') }}
{% endblock %}
